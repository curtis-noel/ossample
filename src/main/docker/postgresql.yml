version: '2'
services:
    ossample-postgresql:
        image: postgres:9.5.4
        # volumes:
        #     - ~/volumes/jhipster/ossample/postgresql/:/var/lib/postgresql/
        environment:
            - POSTGRES_USER=ossample
            - POSTGRES_PASSWORD=
        ports:
            - 5432:5432
